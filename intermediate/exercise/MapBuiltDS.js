// create the map 
//set these  properties addEventListener,b,c
//set these values 1,2,3
// get a from that map
// delete b, then get the size

const numbers = new Map();
// store the nums 
numbers.set('a',10);
numbers.set('b',2);
numbers.set('c',3);

console.log(numbers.get('a'))
