console.dir(document)// which will return all the properties

console.log(document.head) // return the head element

console.log(document.body) // retun the body element

console.log(document.title) // return the title of the page 

console.log(document.baseURI)// retuen the base URL of the local host of the current page 

// if console.log not works then go for console.dir
//Node.js runs JavaScript outside the browser, 
//so it does not have access to browser-specific objects like document, window, or alert.

